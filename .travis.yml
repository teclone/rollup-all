language: node_js
cache:
  directories:
    - ~/.npm
notifications:
  email: false
node_js:
  - "9"
  - "8"
script:
  - npm run test
jobs:
  include:
    - stage: release
      node_js: "11.12.0"
      script:
        - npm run test
        - npm run build
        - ls -a
      after_success:
        - ls -a && npm run report-coverage
        - ls && npm run semantic-release
